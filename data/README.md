# Data

The public repository intentionally excludes real project contracts and institution-specific portfolio datasets.

`sample-benefits-catalog.json` is a synthetic demonstration catalog that preserves the structure required to understand the prototype without exposing organizational information.

For a private deployment, replace this file with an approved benefits catalog and update the relevant Langflow `Read File` node.
